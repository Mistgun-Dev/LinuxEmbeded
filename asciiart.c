#include <stdio.h>
#include <stdlib.h>
#include <assert.h>

int main()
{
    //FILE *file = fopen("in", "r");
    //assert(file);
    int c;
    
    fprintf(stderr, "\nASCII ART -----------------------------------------\n");
   /* while((c = fgetc(file)) != EOF)
    {
    	fprintf(stderr,"%c", c);
    }
    
    fprintf(stderr,"\n");
    fclose(file);
*/
    return 0;
}
